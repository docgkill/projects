#include "Player.h"#include "SimpleAudioEngine.h"#define ACCELERATION 0.05using namespace CocosDenshion;Player::~Player(){        CC_SAFE_RELEASE(_rideAnimation);    CC_SAFE_RELEASE(_floatAnimation);}Player::Player() {    _screenSize = CCDirector::getInstance()->getWinSize();    	_floatingTimerMax = 2;	_floatingTimer = 0;    _speed = PLAYER_INITIAL_SPEED;    _maxSpeed = PLAYER_INITIAL_SPEED;    _floating = false;    _nextPosition = Point(0,0);	_nextPosition.y = _screenSize.height * 0.8f;	_state = kPlayerMoving;    _jumping = false;    _hasFloated = false;}Player * Player::create () {        Player * player = new Player();	if (player && player->initWithSpriteFrameName("player_1")) {        player->getTexture()->setAliasTexParameters();		player->autorelease();        player->setSize();        player->initPlayer();        return player;	}	CC_SAFE_DELETE(player);	return NULL;}void Player::update (float dt) {    if (_speed + ACCELERATION <= _maxSpeed) {        _speed += ACCELERATION;    } else {        _speed = _maxSpeed;    }        _vector.x = _speed;    	switch (_state) {		case kPlayerMoving:			_vector.y -= MY_GRAVITY;               //         if(this->getActionByTag(kPlayerMoving)==NULL)  {     //           this->stopAllActions();       //         this->runAction(_rideAnimation);         //   }                        if (_hasFloated) _hasFloated = false;		break;		        case kPlayerFalling:			//&& _vector.y < 0            if (_floating ) {				_vector.y -= FLOATNG_GRAVITY;				_vector.x *= FLOATING_FRICTION;            } else {          //      if(this->getActionByTag(kPlayerFalling)==NULL)  {            //        this->stopAllActions();              //      this->runAction(_jumpAnimation);              //  }				_vector.y -= MY_GRAVITY;				_vector.x *= AIR_FRICTION;				_floatingTimer = 0;                log("y %f", _vector.y);                if(_vector.y>=10)  {                    this->setSpriteFrame("player_9");                } else if(_vector.y>0 && _vector.y<10)  {                    this->setSpriteFrame("player_5");                }else {                    this->setSpriteFrame("player_12");                }			}		break;        case kPlayerDying:            _vector.y -= MY_GRAVITY;            _vector.x = -_speed;            this->setPositionX(this->getPositionX() + _vector.x);            this->setSpriteFrame("player_4");            this->setRotation(-30.0f);        break;			}        if (_jumping) {        _state = kPlayerFalling;        _vector.y += PLAYER_JUMP * 0.25f;        log("_jumping true %f", _vector.y);        if (_vector.y > PLAYER_JUMP ) _jumping = false;    }        if (_vector.y < -TERMINAL_VELOCITY) _vector.y = -TERMINAL_VELOCITY;      	_nextPosition.y = this->getPositionY() + _vector.y;            	if (_vector.x * _vector.x < 0.01) _vector.x = 0;	if (_vector.y * _vector.y < 0.01) _vector.y = 0;		if (_floating) {		_floatingTimer += dt;		if (_floatingTimer > _floatingTimerMax) {            _floatingTimer = 0; //           SimpleAudioEngine::getInstance()->playEffect("falling.wav");			this->setFloating(false);		}	}}void Player::reset () {        _speed = PLAYER_INITIAL_SPEED;    _maxSpeed = PLAYER_INITIAL_SPEED;        _vector = Point(0,0);    this->setFloating(false);    this->setRotation(0);//    _nextPosition.y = _screenSize.height * 0.6f;    _nextPosition.y = _screenSize.height;    this->setPosition(Point( _screenSize.width * 0.2f, _nextPosition.y ));    _state = kPlayerMoving;    _jumping = false;    _hasFloated = false;}void Player::setFloating (bool value) {        if (_floating == value) return;        if (value && _hasFloated) return;        _floating = value;        this->stopAllActions();        if (value) {        _hasFloated = true;  //      SimpleAudioEngine::getInstance()->playEffect("openUmbrella.wav");  //      this->setDisplayFrame(SpriteFrameCache::getInstance()->getSpriteFrameByName("player_float"));                this->runAction(_floatAnimation);        _vector.y += PLAYER_JUMP * 0.5f;    } else {                this->runAction(_rideAnimation);    }}void Player::initPlayer () {        this->setAnchorPoint(Point(0.5f, 1.0f));    this->setPosition(Point(_screenSize.width * 0.2f, _nextPosition.y));//    _height = 252 * 0.95f;    // 그림 사이즈에 따라 변동    _height = 330;    _width = 184;        Animation* animation;    animation = Animation::create();    SpriteFrame * frame;    int i;    for(i = 1; i <= 12; i++) {        char szName[100] = {0};        sprintf(szName, "player_%i", i);        frame = SpriteFrameCache::getInstance()->getSpriteFrameByName(szName);        animation->addSpriteFrame(frame);    }        animation->setDelayPerUnit(1.0f / 12.0f);    animation->setRestoreOriginalFrame(false);    animation->setLoops(-1);    _rideAnimation = Animate::create(animation);    _rideAnimation->retain();    _rideAnimation->setTag(kPlayerMoving);           Animation* floatAnimation = animation->clone();    floatAnimation->setDelayPerUnit(0.5f/12.0f);    _floatAnimation = CCAnimate::create(floatAnimation);    _floatAnimation->retain();    _floatAnimation->setTag(kPlayerFalling);              /*    animation->setDelayPerUnit(<#float delayPerUnit#>)    _jumpAnimation = _rideAnimation->clone();    _jumpAnimation->retain();    _jumpAnimation->    */            /*    FiniteTimeAction* easeSwing = CCSequence::create(           CCEaseInOut::create(CCRotateTo::create(0.8f, -10), 2),           CCEaseInOut::create(CCRotateTo::create(0.8f, 10), 2),           NULL);    _floatAnimation = RepeatForever::create( (ActionInterval*) easeSwing );    _floatAnimation->retain();    */    this->runAction(_rideAnimation);}